module Types where

type Element = Int          -- Elements will be used to denote elements in a galois field
type Poly = [Element]       -- Polynomials are represented as [x^0, x^1, ...]
type Bits = [Bool]          -- Binary numbers are represented as [2^0, 2^1, ...]
